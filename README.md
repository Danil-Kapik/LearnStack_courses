# LearnStack 🎓

Учебный сайт на Django, реализующим систему курсов и студентов.

## 🔧 Структура проекта

- `LearnStack/` – конфигурационный пакет Django (settings, URLs, WSGI/ASGI и т.п.).
- `apps/` – собственные приложения:
  - `courses` – модели курсов и модулей, API, шаблоны управления курсами, команды управления (загрузка категорий/тегов), тесты.
  - `students` – регистрация/авторизация студентов, просмотр и запись на курсы, соответствующие шаблоны.
  - `theme` – статические файлы, Tailwind‑конфигурация, базовые шаблоны.
- `media/` и `static/` – файлы, загружаемые пользователями и собранные статические ресурсы.
- `manage.py`, `pyproject.toml`, `requirements.txt` – стандартные файлы для запуска и управления зависимостями.

## ⚙️ Установка

1. Клонировать репозиторий.
2. Создать и активировать виртуальное окружение (`.venv`).
3. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Применить миграции и загрузить фикстуры (`apps/courses/fixtures` при необходимости):
   ```bash
   python manage.py migrate
   python manage.py loaddata apps/courses/fixtures/courses.json
   ```
5. Запустить сервер:
   ```bash
   python manage.py runserver
   ```

## 🧩 Особенности

- Модульная архитектура с разделением логики по приложениям.
- Админка с управлением курсами и модулями.
- REST‑API (`apps/courses/api/`).
- Генерация slug’ов, миксины, шаблонные теги.
- Tailwind CSS для стилизации через `theme/static_src`.

## ⚡ Возможности приложения

- Регистрация и авторизация пользователей (студентов) через стандартные формы.
- Просмотр каталога курсов: список курсов и детальная страница курса.
- Управление курсами (создание, редактирование, удаление) через интерфейс управления и отдельные формы/шаблоны.
- Структура курса состоит из модулей; каждый модуль содержит контент разных типов: текст, изображение, файл, видео (реализованы шаблоны в `templates/courses/content/`).
- Запись студентов на курсы (подписка/регистрация на курс).
- Административные команды для загрузки категорий и тегов (`load_categories`, `load_tags`).
- Фикстуры для начального наполнения (`apps/courses/fixtures/`).
- REST‑API для получения списков курсов и деталей курса (`apps/courses/api/`).
- Генерация человекочитаемых slug’ов для курсов и предметов.
- Поддержка загрузки и хранения медиа (изображения, файлы) через `media/`.

## 📁 Разработка

- Добавляйте модели и миграции в соответствующие приложения.
- Пишите тесты в `tests.py` для каждого приложения.
- Команды управления (`load_categories`, `load_tags`) находятся в `apps/courses/management/commands`.

## ℹ️ Контакты и лицензия

(Здесь можно указать автора, контакт и условия лицензирования проекта.)

---

> Этот документ можно расширять по мере развития проекта: требования, деплой, CI‑pipelines и др.
